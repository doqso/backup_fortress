<Window x:Class="WindowApp.DeleteBucketWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindowApp"
        mc:Ignorable="d"
        Title="Borrar contenedor" Height="100" Width="350">
  <Grid Margin="5" VerticalAlignment="Center">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="5"/>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="5"/>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="2"/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Label x:Name="laService" Content="Servicio"/>

    <ComboBox x:Name="cbBuckets" Grid.Column="2"/>

    <Button x:Name="btDeleteBucket" Content="Borrar" Grid.Column="4" Width="60" Click="btDeleteBucket_Click"/>
    <Button x:Name="btCancel" Content="Cancelar" Grid.Column="6" Width="60" Click="btCancel_Click"/>
  </Grid>
</Window>
